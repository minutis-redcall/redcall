{% extends 'base.html.twig' %}

{% block title %}{{ 'sandbox.fake_call.title'|trans }}{% endblock %}

{% block body %}

    <h1>{{ block('title') }}</h1>

    <table class="table">
        {% for row in phones %}
            {% if row.volunteer %}
                <tr>
                    <td>
                        {{ row.volunteer.firstName }} {{ row.volunteer.lastName }}
                        <a href="{{ path('sandbox_fake_call_read', {e164: row.phoneNumber}) }}">{{ row.phoneNumber }}</a>
                        <span class="badge badge-primary">{{ row.countMsg }}</span>
                    </td>
                </tr>
            {% endif %}
        {% endfor %}
    </table>

    <div class="text-center">
{#        <a href="{{ path('sandbox_fake_call_clear', {csrf: csrf_token('fake_call')}) }}"#}
{#           class="btn btn-danger">{{ 'sandbox.fake_call.clear'|trans }}</a>#}

{#        &nbsp;&nbsp;&nbsp;&nbsp;#}

        <a href="{{ path('sandbox_home') }}"
           class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>
    </div>

    <br/>

{% endblock %}
