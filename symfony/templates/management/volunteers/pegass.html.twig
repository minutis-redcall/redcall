{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ volunteer.displayName }} ({{ volunteer.nivol }})</h1>
    <h3 class="color-grey">{{ entity.updatedAt|date('d/m/Y H:i') }}</h3>

    <div class="jumbotron">
        <pre><code>{{ pegass|nl2br }}</code></pre>
    </div>

{% endblock %}