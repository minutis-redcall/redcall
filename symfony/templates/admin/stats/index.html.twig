{% extends 'base.html.twig' %}

{% block body %}

    <div class="jumbotron text-center">

        <a class="text-lg-center large-button btn btn-secondary"
           href="{{ path('admin_stats_general') }}">{{ 'admin.statistics.buttons.general'| trans }}</a>

        <br/><br/>

        <a href="{{ path('admin_stats_structure') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'admin.statistics.buttons.structure'|trans }}</a>

        <br/><br/><br/>

        <a href="{{ path('admin_home') }}"
           class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>

    </div>

{% endblock %}