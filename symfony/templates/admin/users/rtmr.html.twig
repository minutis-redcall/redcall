{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.users.rtmr.title'|trans }}{% endblock %}

{% block body %}
    <div class="col-md-12 rc-section">
        <h3>{{ 'admin.users.rtmr.title'|trans }}</h3>

        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>{{ 'admin.users.rtmr.user'|trans }}</th>
                        <th>{{ 'admin.users.rtmr.structures'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for volunteer in volunteers %}
                        {% set rtmr_user = redcall_user(volunteer) %}
                        <tr>
                            <td>
                                <strong>{{ volunteer.displayName }}</strong><br/>
                                <small class="text-muted">{{ rtmr_user ? rtmr_user.username : '' }}</small>
                            </td>
                            <td>
                                <ul class="list-unstyled mb-0">
                                    {% for structure in (rtmr_user ? rtmr_user.structures : []) %}
                                        <li><i class="fas fa-check-circle text-success mr-2"></i>{{ structure.name }}</li>
                                    {% else %}
                                        <li class="text-muted italic">{{ 'admin.users.rtmr.no_structures'|trans }}</li>
                                    {% endfor %}
                                </ul>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="2" class="text-center text-muted italic">
                                {{ 'admin.users.rtmr.none'|trans }}
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="text-center mt-4">
            <a href="{{ path('admin_redcall_users_index') }}" class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>
        </div>
    </div>
{% endblock %}
