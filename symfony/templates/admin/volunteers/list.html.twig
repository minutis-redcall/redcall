{% extends 'base.html.twig' %}

{% import 'macros.html.twig' as macros %}

{% block body %}

    <h1>
        {{ 'manage_volunteers.title'|trans }}
    </h1>

    <div class="text-center skip-line">
        <a href="{{ path('admin_volunteers_run', app.request.query.all|merge({csrf: csrf_token('manage_volunteers')})) }}"
           onclick="$(this).addClass('disabled');"
           class="text-lg-center large-button btn btn-secondary">
            {{ 'manage_volunteers.import_again'|trans }}
            <br/>
            {{ 'manage_volunteers.import_again_help'|trans }}
        </a>
    </div>

    <div class="text-right skip-line">

        <a href="{{ path('admin_volunteers_create') }}" class="btn btn-secondary">
            {{ 'manage_volunteers.form.create'|trans }}
        </a>

    </div>

    <nav>
        <div class="nav nav-tabs" id="nav-tab" role="tablist">
            <a class="nav-item nav-link active" id="nav-enabled-tab" data-toggle="tab"
               href="#nav-enabled" role="tab"
               aria-controls="nav-enabled" aria-selected="true">{{ 'manage_volunteers.tabs.enabled'|trans }}</a>
            <a class="nav-item nav-link" id="nav-disabled-tab"
               data-toggle="tab"
               href="#nav-disabled" role="tab"
               aria-controls="nav-disabled" aria-selected="false">{{ 'manage_volunteers.tabs.disabled'|trans }}</a>
        </div>
    </nav>

    <div class="tab-content" id="nav-content">
        {% for type, table in data %}

            <div class="tab-pane {% if type == 'enabled' %}show active{% endif %}"
                 id="nav-{{ type }}"
                 role="tabpanel" aria-labelledby="nav-{{ type }}-tab">

                {{ macros.orderBy(table.orderBy, {
                    'nivol': 'manage_volunteers.order.nivol'|trans,
                    'firstName': 'manage_volunteers.order.firstname'|trans,
                    'lastName': 'manage_volunteers.order.lastname'|trans,
                    'lastPegassUpdate': 'manage_volunteers.order.last_pegass_update'|trans,
                    'locked': 'manage_volunteers.order.locked'|trans,
                }, 'nav-' ~ type) }}

                <table class="table">
                    <tbody>
                    {% for volunteer in table.rows %}

                        <tr>
                            <td>
                                <div>{{ 'manage_volunteers.columns.nivol'|trans }}: {{ volunteer.nivol }}</div>
                                <div>{{ 'manage_volunteers.columns.firstname'|trans }}: {{ volunteer.firstname }}</div>
                                <div>{{ 'manage_volunteers.columns.lastname'|trans }}: {{ volunteer.lastname }}</div>
                                <div>{{ 'manage_volunteers.columns.phone'|trans }}: {{ volunteer.phoneNumber }}</div>
                                <div>{{ 'manage_volunteers.columns.email'|trans }}: {{ volunteer.email }}</div>
                                <div>{{ 'manage_volunteers.columns.minor'|trans }}: {{ volunteer.isMinor ? '🚩' : '❌' }}</div>
                                <div>{{ 'manage_volunteers.columns.callable'|trans }}: {{ volunteer.isCallable ? '✅' : '❌' }}</div>
                            </td>
                            <td>
                                {% for tag in constant('App\\Entity\\Tag::TAGS') %}
                                    <div>{{ ('tag.' ~ tag)|trans }}: {{ volunteer.hasTag(tag) ? '✅' : '❌' }}</div>
                                {% endfor %}
                            </td>
                            <td>
                                {% if volunteer.report %}
                                    {% for error in volunteer.report %}
                                        <div><code>{{ error }}</code></div>
                                    {% endfor %}
                                {% else %}
                                    &nbsp;
                                {% endif %}
                            </td>
                            <td>
                                {% if volunteer.lastPegassUpdate and volunteer.lastPegassUpdate|date('Y') != 2000 %}
                                    {{ 'manage_volunteers.columns.update'|trans({'%date%': volunteer.lastPegassUpdate|date('d/m/Y')}) }}
                                {% else %}
                                    {{ 'manage_volunteers.columns.never_updated'|trans }}
                                {% endif %}
                            </td>
                            <td>
                                {# Refresh volunteer general data #}
                                <a href="{{ path('admin_volunteers_refresh_general', app.request.query.all|merge({volunteerId: volunteer.id, csrf: csrf_token('manage_volunteers')})) }}" onclick="$(this).addClass('disabled');">
                                    {{ 'manage_volunteers.action.refresh_general'|trans }}
                                </a>
                                <br/>

                                {# Refresh volunteer skills #}
                                <a href="{{ path('admin_volunteers_refresh_skills', app.request.query.all|merge({volunteerId: volunteer.id, csrf: csrf_token('manage_volunteers')})) }}" onclick="$(this).addClass('disabled');">
                                    {{ 'manage_volunteers.action.refresh_skills'|trans }}
                                </a>
                                <br/><br/>

                                {# Lock volunteer #}
                                {% if volunteer.locked %}
                                    <a href="{{ path('admin_volunteers_unlock', app.request.query.all|merge({volunteerId: volunteer.id, csrf: csrf_token('manage_volunteers')})) }}"
                                       class="btn btn-warning">
                                        {{ 'manage_volunteers.action.unlock'|trans }}
                                    </a>
                                {% else %}
                                    <a href="{{ path('admin_volunteers_lock', app.request.query.all|merge({volunteerId: volunteer.id, csrf: csrf_token('manage_volunteers')})) }}"
                                       class="btn btn-danger">
                                        {{ 'manage_volunteers.action.lock'|trans }}
                                    </a>
                                {% endif %}

                                {# Disable volunteer #}
                                {% if not volunteer.locked %}
                                    {% if volunteer.enabled %}
                                        <a href="{{ path('admin_volunteers_disable', app.request.query.all|merge({volunteerId: volunteer.id, csrf: csrf_token('manage_volunteers')})) }}"
                                           class="btn btn-danger">
                                            {{ 'manage_volunteers.action.disable'|trans }}
                                        </a>
                                    {% else %}
                                        <a href="{{ path('admin_volunteers_enable', app.request.query.all|merge({volunteerId: volunteer.id, csrf: csrf_token('manage_volunteers')})) }}"
                                           class="btn btn-success">
                                            {{ 'manage_volunteers.action.enable'|trans }}
                                        </a>
                                    {% endif %}
                                {% endif %}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

            </div>

        {% endfor %}
    </div>

{% endblock %}