{% extends 'base.html.twig' %}

{% block body %}

    <div class="rc-section text-center">

        {{ form(form) }}

        <div class="rc-back-link">
            <a href="{{ path('admin_maintenance_index') }}"
               class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>
        </div>

    </div>

{% endblock %}