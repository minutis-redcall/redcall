{% extends 'base.html.twig' %}

{% block menu %}{% endblock %}

{% block body %}

    <div class="jumbotron text-center">

        <a href="{{ path('admin_maintenance_refresh') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'maintenance.refresh'|trans }}</a>

        <br/><br/>

        <a href="{{ path('admin_maintenance_refresh_all') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'maintenance.refresh_all'|trans }}</a>

        <br/><br/><br/>

        <a href="{{ path('admin_home') }}"
           class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>

    </div>

{% endblock %}