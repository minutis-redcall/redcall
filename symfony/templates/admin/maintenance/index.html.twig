{% extends 'base.html.twig' %}
{% import 'macros.html.twig' as macros %}

{% block body %}

    <br/>
    <div class="jumbotron text-center">

        <a class="text-lg-center large-button btn btn-secondary" href="{{ path('admin_maintenance_refresh') }}">{{ 'maintenance.refresh'| trans }}</a>

        <br/><br/>

        <a class="text-lg-center large-button btn btn-secondary" href="{{ path('admin_maintenance_pegass_files') }}">{{ 'maintenance.pegass'| trans }}</a>

        <br/><br/>
        <a href="{{ path('admin_maintenance_search') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'maintenance.search.title'|trans }}</a>

        <br/><br/>

        <a href="{{ path('admin_maintenance_message') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'maintenance.message.title'|trans }}</a>

        <br/><br/><br/>

        <a href="{{ path('admin_home') }}"
           class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>

    </div>

{% endblock %}