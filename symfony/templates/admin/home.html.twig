{% extends 'base.html.twig' %}

{% block menu %}{% endblock %}

{% block body %}

    <div class="jumbotron text-center">

        <a href="{{ path('admin_pegass_index') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'password_login.user_list.button'|trans }}</a>

        <br/><br/>

        <a href="{{ path('admin_prefilled_answers_list') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'prefilled_answers.title'|trans }}</a>

        <br/><br/>

        <a href="{{ path('admin_campaign_index') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'campaign.titles.list_admin' | trans }}</a>

        <br><br>

        <a href="{{ path('admin_dashboard_statistics') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'admin.dashboard.title' |trans }}</a>

        <br><br>

        <a href="{{ path('admin_maintenance_index') }}"
           class="text-lg-center large-button btn btn-secondary">{{ 'maintenance.title'|trans }}</a>

        <br/><br/><br/>

        <a href="{{ path('home') }}"
           class="btn btn-secondary">{{ 'base.button.back'|trans }}</a>

    </div>

{% endblock %}